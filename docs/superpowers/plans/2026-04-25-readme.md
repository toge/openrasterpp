# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a Japanese `README.md` for `openrasterpp` that covers overview, requirements, build/test flow, CMake install usage, and a short usage example.

**Architecture:** Add a single root-level `README.md` and keep all information aligned with the current repository state. Verification is content-based: confirm required sections, commands, and CMake usage snippets exist and match the public API and current build files.

**Tech Stack:** Markdown, CMake, shell commands, ripgrep

---

## File map

- Create: `README.md`
  - Japanese project overview and developer/user instructions.
- Reference: `CMakeLists.txt`
  - Source of truth for project version, dependencies, install/export support.
- Reference: `openraster.hpp`
  - Source of truth for documented public API names.
- Reference: `build.sh`, `test.sh`
  - Source of truth for documented local build/test flows.

### Task 1: Create the README structure

**Files:**
- Create: `README.md`
- Test: `README.md`

- [ ] **Step 1: Write the content checklist first**

Required README sections:

```text
# openrasterpp
## 概要
## 主な機能
## 必要要件
## 依存関係
## ビルド方法
## テスト方法
## インストールと利用方法
## 使用例
```

- [ ] **Step 2: Verify the README does not exist yet**

Run:

```bash
test ! -f README.md
```

Expected: PASS

- [ ] **Step 3: Write the initial README.md**

Include:

1. Japanese overview describing `openrasterpp` as an OpenRaster I/O library.
2. Current dependencies from `CMakeLists.txt`.
3. Build/test commands consistent with repository files.
4. Installed-package CMake example using:

```cmake
find_package(openrasterpp CONFIG REQUIRED)
target_link_libraries(my_app PRIVATE openrasterpp::openrasterpp)
```

5. An install command example using:

```bash
cmake --install build --prefix "$PWD/build/install-prefix"
```

6. A note that downstream consumers must make both `openrasterpp` and its
   dependencies discoverable through `CMAKE_PREFIX_PATH` or equivalent package locations.

7. Short C++ example using:

```cpp
ora::ImageBuffer::make_blank(...)
ora::OraDocument
ora::write(...)
ora::read(...)
```

- [ ] **Step 4: Verify the section structure exists**

Run:

```bash
rg -n "^# openrasterpp|^## 概要|^## 主な機能|^## 必要要件|^## 依存関係|^## ビルド方法|^## テスト方法|^## インストールと利用方法|^## 使用例" README.md
```

Expected: PASS with one match per required heading.

### Task 2: Verify README content against the repository

**Files:**
- Modify: `README.md`
- Test: `README.md`, `CMakeLists.txt`, `openraster.hpp`

- [ ] **Step 1: Check CMake usage snippets**

Run:

```bash
rg -n "find_package\\(openrasterpp CONFIG REQUIRED\\)|openrasterpp::openrasterpp|CMAKE_PREFIX_PATH|cmake --install build --prefix" README.md
```

Expected: PASS with the downstream usage snippet, install command, and dependency discovery note present.

- [ ] **Step 2: Check API names used in the example**

Run:

```bash
rg -n "ImageBuffer::make_blank|OraDocument|ora::write|ora::read" README.md
```

Expected: PASS with all four API references present.

- [ ] **Step 3: Check that documented dependencies match the project**

Run:

```bash
rg -n "minizip-ng|lodepng|Catch2|C\\+\\+26|CMake 3\\.25" README.md
```

Expected: PASS with all required dependency/version references present.

- [ ] **Step 4: Check that local build and test commands are documented**

Run:

```bash
rg -n "sh ./build.sh|sh ./test.sh|cmake --install build --prefix" README.md
```

Expected: PASS with the documented local workflow present and aligned with the current repository scripts.

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: add Japanese README"
```
